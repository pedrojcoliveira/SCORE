@model IEnumerable<SCORE.Models.ExercicioUc>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdExercicioNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdUcNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdExercicioNavigation.IdExercicio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdUcNavigation.IdUc)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdExercicioUc">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdExercicioUc">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdExercicioUc">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

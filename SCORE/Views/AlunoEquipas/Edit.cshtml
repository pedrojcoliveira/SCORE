@model SCORE.Models.AlunoEquipa

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>AlunoEquipa</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdAlunoEquipa" />
            <div class="form-group">
                <label asp-for="NmecanograficoAluno" class="control-label"></label>
                <select asp-for="NmecanograficoAluno" class="form-control" asp-items="ViewBag.NmecanograficoAluno"></select>
                <span asp-validation-for="NmecanograficoAluno" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IdEquipa" class="control-label"></label>
                <select asp-for="IdEquipa" class="form-control" asp-items="ViewBag.IdEquipa"></select>
                <span asp-validation-for="IdEquipa" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IdTurma" class="control-label"></label>
                <input asp-for="IdTurma" class="form-control" />
                <span asp-validation-for="IdTurma" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
